# AF_XDP化の実装・計測結果 (2026-07-05)

`performance-up-thread-xdp` ブランチで実施。`plan.md` の計画に基づき、AF_PACKETをAF_XDP(copyモード)に置き換えた。

## 実装内容

### 追加ファイル
- `src/c/xdp_filter.bpf.c` — XDPプログラム。EtherType `0x0800`(IPv4)のうち、**宛先IPがそのNIC自身のIPと一致しないパケットだけ**をXSKMAP(AF_XDPソケット)へredirectする。ARP、および自ノード宛てのIPv4パケット(ping/ssh等)は`XDP_PASS`でカーネルの通常経路に流す。
- `src/c/xdpphp.c` — libxdp/libbpfを使うユーザー空間FFIシム。BPFプログラムのロード/アタッチ、UMEM+fill/completion/rx/txリングの管理、`xdpphp_open/close/recv/send/last_error`という最小APIをPHPのFFIに提供する。フレームの再利用(fill ring/completion ring)はすべてこの層で完結し、PHP側には見せない。
- `src/c/Makefile` — `libxdpphp.so`と`xdp_filter.bpf.o`をビルド。
- `src/Xdp/XskSocket.php` — 上記CライブラリをラップするFFIクラス。NICごとに1インスタンス。

### 変更ファイル
- `docker/router/Dockerfile` — `libxdp-dev libbpf-dev libelf-dev zlib1g-dev clang llvm pkg-config bpftool`を追加、`ffi`拡張を有効化(`ffi.enable=true`)。
- `docker/router/mnt/entrypoint.sh` — コンテナ起動時に`make -C /router/src/c`でCコードをビルド(リポジトリ全体がbind mountされるため、イメージビルド時ではなくコンテナ起動時にビルドする方式)。
- `src/Router.php` — `socket_create(AF_PACKET,...)`/`socket_recv`/`socket_write`を`XskSocket`に置換。`parallel\Runtime`/`Channel`によるwriteスレッドオフロードは廃止し、単一プロセス・単一ループに簡略化(理由はplan.md参照)。

### 実装中に見つけて直したバグ
1. **ARP/自ノード宛てトラフィックの消失**: 当初「IPv4なら無条件にXSKMAPへredirect」としていたところ、XDPのredirectは複製ではなく「奪う」操作のため、ping(ICMP over IPv4)を含むルーター自身宛てのトラフィックまでカーネルのIPスタックに届かなくなり、alice→router自身へのpingすら失敗した。宛先IPがそのNIC自身のIPと一致する場合は`XDP_PASS`するBPF側のマップ(`self_ip4`)を追加して解決。
2. **メモリリーク**: `XskSocket::sendFrame()`内で`FFI::new("unsigned char[$len]", false)`と、第2引数(owned)を誤って`false`にしていたため、送信フレームごとにネイティブバッファがリークしていた。UDP高負荷試験で数十秒のうちにPHPのデフォルト`memory_limit`(128MB)を使い切りFatal errorでプロセスがクラッシュした。`false`を除去(デフォルトのowned=trueに戻す)して解決。

## 計測方法

環境: docker-compose上のalice(10.0.0.10) → router(eth0=10.0.0.250/eth1=10.0.1.250, privileged) → bob(10.0.1.10)。docker bridgeネットワーク経由のveth(net1/net2)。alice/bobは`ethtool -K eth0 rx off tx off tso off gro off`適用済み(entrypoint.shで自動)。

- AF_XDP版: `performance-up-thread-xdp`ブランチ(本実装)
- 比較対象(ベースライン): `performance-up-thread`ブランチ(AF_PACKET + `parallel`拡張によるwriteスレッドオフロード版。過去のコミットログで「3.14Gbpsまでいった」と記録されている構成)

両ブランチとも同一のdocker-composeスタック上で、`git checkout`でブランチを切り替え→`docker compose build router && docker compose up -d --force-recreate router`→ルーター起動→計測、という同一手順で直接比較した。

実行したコマンド:
```
# UDP (alice → bob, 4並列)
iperf3 -c 10.0.1.10 -u -b 700M -P 4 -t 10

# TCP (alice → bob, 2並列)
iperf3 -c 10.0.1.10 -P 2 -t 10
```

## 計測結果

| 項目 | AF_PACKET版 (parallelスレッド、ベースライン) | AF_XDP版 (単一スレッド、今回) |
|---|---|---|
| UDP 700M×4並列 (受信側) | 2.65 Gbps、loss 1.4% (31,714 / 2,324,940 datagrams) | 1.56 Gbps、loss 44% (1,058,795 / 2,416,908 datagrams) |
| TCP -P 2 (受信側) | 1.45 Gbps、retransmit 1,779 | 1.03 Gbps、retransmit 105 |

いずれの計測でも、ルーターのPHPプロセスはAF_XDP版・AF_PACKET版とも試験中クラッシュせず完走(メモリリーク修正後)。AF_XDP版のCPU使用率はrouterコンテナ内で常時ほぼ100%(1コア、busy-spin方式のため想定通り)。

## 考察

**このveth環境では、AF_XDP版は既存のAF_PACKET+parallelスレッド版より遅いという結果になった。**

主な要因と考えられるもの:

1. **zero-copyが使えない制約**: 事前検証の通り、vethはDMA非対応のソフトウェアデバイスのため、AF_XDPはcopyモードでしか動作しない。「skb確保とフルスタック通過を回避できる」というAF_XDP本来のメリットの一部(コピー自体の削減)がこの環境では発揮されない。
2. **並列度の後退**: 今回の実装ではAF_XDPのTXがブロッキングsyscallでなくリング操作である点を踏まえ、`parallel\Runtime`による書き込みスレッドのオフロードをあえて廃止し単一プロセス・単一ループにした(plan.mdの設計判断)。一方ベースライン側はwrite処理を別スレッドに逃がすことで実質的に複数コアを使えていた。今回の結果は、「AF_XDPによるカーネル経路の短縮」より「マルチスレッドによる複数コア活用」の効果の方がこの環境では大きかったことを示している。
3. **単一コアでのbusy-spinがボトルネック**: AF_XDP版はCPU使用率が常時100%(1コア)に張り付いており、明らかにCPUバウンドで頭打ちになっている。recv/route/ARP/checksum/send一式を1コアで捌ききれていない。

## 今後の選択肢

- **Option A (plan.mdに記載済み、未実装)**: `xsk_socket__create_shared`で複数の`parallel\Runtime`ワーカー間でUMEMを共有し、AF_XDP版でも並列化する。実装難易度は上がるが、今回の結果を見る限りこちらを試さない限りAF_PACKET版を上回るのは難しそうである。
- busy-spinではなく`xdpphp_fd()`を公開して`poll()`待機にし、CPU使用率を下げつつマルチプロセス/マルチスレッド化と組み合わせる。
- そもそもこの veth+copyモードという条件下でAF_XDPを使う効果は限定的なので、実NIC(zero-copy対応ドライバ)環境での再検証も比較材料として有用。
