# write用workerCount=2の計測結果 (2026-07-22)

`performance-up-thread-xdp`ブランチ(read=AF_XDP / write=AF_PACKET+`parallel`スレッド, hybrid構成)で、`src/Router.php`の`workerCount`を`1`→`2`に変更し、write用の`parallel`スレッド(AF_PACKET書き込みソケット)を2本に増やして計測した。

## 変更内容

- `src/Router.php`: `private int $workerCount = 1;` → `private int $workerCount = 2;` のみ変更。他の実装は変更なし。
- `start()`内の書き込み先スレッド振り分けは既存のまま: パケットごとに`$chan[$cnt]->send([$dstPkt, $writeDeviceName])`し、`$cnt`を`workerCount`でラウンドロビンする実装(`Router.php`の該当箇所)。**フロー(コネクション)単位ではなくパケット単位の振り分け**であることに注意。

## 計測環境・方法

- `howto-measure.md`の手順に従い、2プロセス構成(`start_eth0.php`+`start_eth1.php`)+JIT有効で計測。RPSは設定していない(デフォルト無効)。
- 比較対象は直前の計測である`result-20260705-2.md`記載の「AF_XDP hybrid 2プロセス+JIT」(workerCount=1相当)。
- 実行コマンドは標準の2本:
  ```
  # UDP (alice → bob, 4並列)
  iperf3 -c 10.0.1.10 -u -b 700M -P 4 -t 10

  # TCP (alice → bob, 2並列)
  iperf3 -c 10.0.1.10 -P 2 -t 10
  ```
- TCPは結果の再現性を確認するため2回計測した。

## 計測結果

| 項目 | workerCount=1(前回, hybrid 2プロセス+JIT) | **workerCount=2(今回)** |
|---|---|---|
| UDP 700M×4並列(受信側) | 2.67 Gbps、loss 0.19% | **2.79 Gbps、loss 0.15%** |
| TCP -P 2(受信側、1回目) | 2.59 Gbps、retransmit 20 | **2.03 Gbps、retransmit 14,388** |
| TCP -P 2(受信側、2回目・再現性確認) | - | **1.99 Gbps、retransmit 12,790** |

## 考察

- **UDPはわずかに改善**した(2.67→2.79Gbps、lossも微減)。UDPはパケット順序に寛容なため、書き込み処理を2スレッドに分散できた分だけ素直にスループットが伸びたと考えられる。
- **TCPは大幅に悪化**した(2.59→約2.0Gbps、retransmitが20から12,000〜14,000超に爆増)。2回計測して同様の傾向を確認しており、偶発的な揺らぎではなく構造的な問題と判断した。

### 原因: パケット単位ラウンドロビンによるフロー内順序崩れ

`Router.php`の書き込み先振り分けは、TCPコネクションやUDPストリームといった「フロー」を意識せず、パケットが来た順に`$cnt`を1つずつ進めて`workerCount`本のチャネルへ交互に配る実装になっている。

- `workerCount=1`のときは書き込みが常に単一スレッド・単一チャネルを通るため、`socket_write`の発行順序(=ワイヤ上の送出順序)は入力順と一致する。
- `workerCount=2`にすると、同一TCPコネクションの連続したパケットが2つの独立した`parallel`スレッドへ交互に割り振られる。各スレッドは非同期に動くため、後から送られたはずのパケットが先に`socket_write`されるなど、ワイヤ上での送出順序が入れ替わる(reordering)ことがある。
- TCPは受信側がこの順序崩れを重複ACK/パケットロストとみなし、fast retransmitを誘発する。これがretransmit急増と実効スループット低下の直接原因と考えられる。
- UDPは順序を気にしないプロトコルのため、同じreorderingが起きてもスループット面では影響が小さく、むしろCPU分散の効果だけが表れた。

## 結論・今後の選択肢

- **現状の実装のまま`workerCount`を2以上に増やすのは、TCPトラフィックを扱う環境では推奨できない。** UDPのみ改善し、TCPは大幅に悪化するトレードオフになる。
- 有効に活用するには、フロー単位(例: 送信元/宛先IP+ポートのハッシュ値)で同一チャネル・同一スレッドに固定する振り分けへ変更し、フロー内の順序を保証する必要がある。その場合、フロー数がworkerCountより十分多くない限りロードバランスの偏りが出る点にも注意が必要。
- 計測後、`workerCount`は`1`に戻した(下記の追記作業の一環)。

## 追記: writeを「別スレッドのままAF_XDP化」する実装と計測

上記のworkerCount実験を踏まえ、`workerCount`は`1`に戻したうえで、write処理自体をAF_PACKETからAF_XDPに置き換える実装を行った。write処理は従来通り`parallel`の別スレッドにオフロードしたままとする。

### 実装方針の検討

AF_XDPは(NIC, queue)ごとに1ソケットしかbindできない制約がある。これまでの2プロセス構成(`start_eth0.php`+`start_eth1.php`)では、各プロセスが自分の担当NICだけAF_XDPで受信し、**他方のNICへの書き込みはAF_PACKETで行う**ことでこの制約を回避していた。この「他方のNICへの書き込み」までAF_XDP化しようとすると、他方のNICは既にもう一方のプロセスがRX用に排他bind済みのため、shared UMEM機構でそのプロセスのfdを共有する必要があり、プロセス間でのfd受け渡し(Unixドメインソケット+SCM_RIGHTS)という新規のIPCが要る本格的な実装になる。

そのため、実装方針をユーザーに確認し、**単一プロセス構成(`start.php`、両NICを1プロセスで処理)のみ対応**する方針を採用した。単一プロセス構成なら両NICのAF_XDPハンドルが同一プロセス内(`parallel`スレッド間)にあるため、書き込みスレッドがそのまま同じハンドルのTX ring/completion ringを使ってAF_XDP送信できる。

### 実装内容

- `src/c/xdpphp.c`:
  - TX ring・completion ring・TX用フレームpool・`xdpphp_send()`を復元(いずれも`result-20260705-2.md`のhybrid化時に一度削除したもの)。
  - `xdpphp_handle_address()`を新設。ハンドルの生アドレス(`uintptr_t`)をそのまま返すだけの関数。
  - ファイル冒頭のコメントに設計意図を明記: recv(rx/fill ring)とsend(tx/comp ring)は完全に別フィールド・別UMEM領域を使うため、**同じハンドルを異なるOSスレッドから同時に呼んでもロック無しで安全**という前提を書いている。
- `src/Xdp/XskSocket.php`:
  - コンストラクタをprivate化し、`open()`(新規オープン)と`fromHandleAddress()`(既存ハンドルの生アドレスから送信専用インスタンスを復元。`xdpphp_close()`は呼ばない)の2つの名前付きコンストラクタに分割。
  - `sendFrame()`(送信)と`getHandleAddress()`(ハンドルアドレス取得)を追加。
- `src/Router.php`:
  - `start()`で、`$this->handleNic === null`(単一プロセス構成)かつ全NIC分のAF_XDPハンドルを保持している場合のみ`$useXdpWrite = true`とし、各NICのハンドルアドレス(int)を`parallel\Runtime::run()`の引数経由でワーカースレッドに渡す。
  - ワーカースレッド側では`XskSocket::fromHandleAddress()`でポインタを再構築し`sendFrame()`で送信。`$useXdpWrite`がfalseの場合(2プロセス構成)は従来通りAF_PACKET+`socket_write()`にフォールバックする。
  - `Runtime`のコンストラクタにcomposerのautoload bootstrapを渡すよう変更(`new Runtime(__DIR__ . '/../vendor/autoload.php')`)。ワーカースレッド内で`Xdp\XskSocket`をオートロードするために必要。

### 事前検証: FFIでのポインタ⇄整数変換とスレッドをまたいだ共有

実装前に、以下をdocker上の実機で単体検証した。

1. `FFI::cast('uintptr_t', $ptr)`の戻り値はCData(スカラーラッパー)で、intを得るには`->cdata`プロパティを読む必要がある。一方、**C関数呼び出しの戻り値**(例: `unsigned long`を返す関数)はPHPのネイティブintとして直接返る(`->cdata`は不要、むしろ付けると誤動作する)。この違いに気づかず一度実装を誤り(`getHandleAddress()`で`->cdata`を付けてしまっていた)、簡易テストで修正した。
2. `parallel\Runtime`のワーカースレッドへ、メインスレッドで取得したポインタの生アドレス(int)を`Channel`/`run()`引数経由で渡し、ワーカー側で`FFI::cast()`により同じアドレスを指すポインタを再構築、C関数を呼び出して正しく動作することを確認した(malloc/calloc由来のメモリは同一プロセス内の全スレッドから有効、という前提が実際に成立することの確認)。

### 動作確認(docker-composeでの実機テスト)

- **単一プロセス構成(`start.php`, AF_XDP write有効)**: alice→router→bobの疎通OK、ルーターPHPプロセスのクラッシュ無し。
  - UDP 700M×4並列(受信側): **2.05 Gbps、loss 1%**
  - TCP -P 2(受信側): **1.36 Gbps、retransmit 13**
- **2プロセス構成(`start_eth0.php`+`start_eth1.php`, AF_PACKETフォールバック)**: 疎通OK、プロセスクラッシュ無し。
  - TCP -P 2(受信側): 1.14 Gbps、retransmit 3(従来経路のまま動作することの確認が目的で、フォールバック狙いの簡易確認のみ)

### 考察

- 実装自体は単一プロセス構成で正しく動作し、read(メインスレッド)とwrite(`parallel`ワーカースレッド)が同じAF_XDPハンドルを介して安全に協調動作することを確認できた。
- ただし、**単一プロセス構成はこれまでの計測でも2プロセス構成よりスループットが低い傾向があり**、今回もその傾向どおりだった。UDPで1%のlossが出るなど、AF_XDP write化そのものによる明確な性能向上は今回の簡易計測では確認できていない。
- 2プロセス構成(これまでの最速構成)ではAF_XDP write化の恩恵を受けられず、引き続きAF_PACKETフォールバックのままである。

### 今後の選択肢

- 単一プロセス構成でのAF_XDP write版を、`howto-measure.md`の標準手順(JIT有効、複数回計測)に沿ってAF_PACKET版と正式に比較する。
- 2プロセス構成でもAF_XDP write化を行うには、Unixドメインソケット+SCM_RIGHTSによるプロセス間fd共有とshared UMEMの実装が必要になる。効果が単一プロセス構成で確認できてから着手を検討する。

## 追記: 単一プロセス構成(AF_XDP write)でJITを有効にした計測

上記のAF_XDP write実装(単一プロセス構成`start.php`)について、`howto-measure.md`記載のJITオプションを付けて再計測した。

```bash
docker compose exec -T router bash -c "
cd /router
nohup php -d ffi.enable=true -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start.php > /tmp/router.log 2>&1 &
disown"
```

TCPは結果の再現性を確認するため2回計測した。

### 計測結果

| 項目 | JIT無効(前掲) | **JIT有効(今回)** |
|---|---|---|
| UDP 700M×4並列(受信側) | 2.05 Gbps、loss 1% | **2.09 Gbps、loss 0.12%** |
| TCP -P 2(受信側、1回目) | 1.36 Gbps、retransmit 13 | **1.74 Gbps、retransmit 716** |
| TCP -P 2(受信側、2回目・再現性確認) | - | **1.75 Gbps、retransmit 750** |

計測後もルーターPHPプロセスは生きており、クラッシュは無かった。

### 考察

- **UDPはlossが1%→0.12%へ改善**し、スループットもわずかに向上した。JITによるパケット処理(Ethernet/IPヘッダのパースやルーティング判定)の高速化が、recvスレッド側の処理落ちを減らした可能性がある。
- **TCPはスループットが1.36→1.75Gbpsへ改善**した一方、**retransmitは13→700程度へ増加**した。2回計測して同様の値で再現しており、偶発的な揺らぎではない。
  - 増加幅は`workerCount=2`実験(retransmit 12,000〜14,000超)ほど極端ではなく、桁が違う。`workerCount=2`のケースは「パケット単位ラウンドロビンによるフロー内順序崩れ」が直接原因だったが、今回は`workerCount=1`のまま(書き込みは常に単一のwriteスレッド)であり、そのメカニズムは働いていないはずである。JIT有効化によってメインスレッド(recv/ルーティング処理)とwriteスレッドの処理速度バランスが変化し、何らかの形でTX側のバックプレッシャー(`xdpphp_send()`のTX ring/completion ringが埋まっている場合は無条件でdropする実装になっている)によるパケットロスが増えた可能性がある。原因は未調査。
- 総じて、JIT有効化はUDP/TCPともスループット面ではプラスに働いたが、TCPのretransmit増加という新たなトレードオフが観測された。

### 今後の選択肢(追記)

- TCPのretransmit増加(JIT有効時)の原因調査。`xdpphp_send()`のバックプレッシャーdrop(TX ring/completion ring枯渇)が実際に発生しているか、C側にドロップ数カウンタを追加して確認するのが次の一手として有力。
- 2プロセス構成+AF_PACKET(これまでの最速構成)とのJIT込みでの正式な比較(UDP/TCPとも)。

## 追記: 単一プロセス構成(AF_XDP write)+JITでのTCP再計測(別セッション)

環境(docker-composeのコンテナ)が一度破棄されていたため再作成し、同じJITオプションで`start.php`を再起動したうえでTCPのみ再計測した(再現性確認のため2回)。alice→bobの疎通は事前に確認済み、計測後もルーターPHPプロセスはクラッシュしていない。

### 計測結果

| 項目 | 前回のJIT計測(本ファイル前掲) | **今回(再計測)** |
|---|---|---|
| TCP -P 2(受信側、1回目) | 1.74 Gbps、retransmit 716 | **898 Mbps、retransmit 0** |
| TCP -P 2(受信側、2回目) | 1.75 Gbps、retransmit 750 | **917 Mbps、retransmit 0** |

今回は前回と傾向が異なり、**retransmitは0だが、iperf3の受信側区間が`0.00-20.0x`秒相当に伸びており**(送信側は`0.00-10.00`秒で1.8Gbps超を報告)、実効スループットは半分程度(約0.9Gbps)に留まった。retransmitによる再送ではなく、送受信間のバッファリング遅延(`result-20260705-2.md`で触れた「バッファブロート」に近い現象)でデータが遅れて完了した可能性が高い。

### 考察: 前回計測との差異について

同じコード・同じ起動コマンドにもかかわらず、前回(retransmit急増型)と今回(retransmit 0だが実効速度半減型)とで異なる劣化パターンが観測された。この揺らぎの主な原因として、**Dockerホスト自体が本計測専用ではなく、本プロジェクトと無関係な他のコンテナ(`router-supplychain-router`、`sc-router`、`container-a`等)が同時に稼働しCPUを奪い合っている**ことが疑われる(`docker ps -a`で確認済み)。これまでの`result-*.md`の計測もこの共有ホスト上で行っており、CPU競合の影響度合いはセッションごとの他コンテナの稼働状況に左右されている可能性がある。

### 今後の選択肢(追記2)

- 計測前に`docker ps -a`で無関係なコンテナの稼働状況を確認し、必要なら一時停止してから計測することで、ホスト側CPU競合によるノイズを減らす。
- 同一条件で3回以上の複数回計測を行い、中央値・ばらつきを記録する運用に変える(現状は多くて2回のみで、今回のように傾向が大きく変わるケースがあるため)。

## 追記: 無関係なコンテナを停止した状態でのTCP再計測

上記で疑われた「共有Dockerホスト上の無関係なコンテナとのCPU競合」を排除するため、`docker ps -a`で確認できた本プロジェクトと無関係なコンテナ(`router-supplychain-router`(`router`)、`container-a`、`sc-router`)を`docker stop`で停止し、本プロジェクトの4コンテナ(`router-container`/`alice-container`/`bob-container`/`perf-container`)のみが稼働している状態にしてから、同じ`start.php`(単一プロセス, AF_XDP write, JIT有効)のままTCPを3回計測した。ルーターPHPプロセスは再起動していない(継続稼働のまま)。

### 計測結果

| 回 | 受信側スループット | Retr(SUM) | 受信側区間 |
|---|---|---|---|
| 1回目 | 1.44 Gbps | 74 | 0.00-10.05秒(バッファブロート無し) |
| 2回目 | 1.45 Gbps | 12 | 0.00-10.05秒(バッファブロート無し) |
| 3回目 | 1.45 Gbps | 25 | 0.00-10.05秒(バッファブロート無し) |

3回とも**1.44〜1.45 Gbps、retransmit 12〜74**と、無関係コンテナ稼働時に観測した2種類の劣化パターン(retransmit 700超 / 実効速度半減のバッファブロート)のどちらも再現せず、値のばらつきも小さくなった。

### 考察

- 無関係なコンテナを止めるだけで、run間のばらつきが大幅に縮小した。これまでの「JIT有効時にretransmitが急増する」「バッファブロートで実効速度が半減する」という観測は、いずれも**ホスト側のCPU競合によるノイズだった可能性が高く**、AF_XDP write実装やJIT自体に起因する構造的な問題ではなかったと考えられる。
- 今回のクリーンな環境での値(1.44〜1.45Gbps)は、JIT無効時の値(1.36Gbps、本ファイル前掲)と近く、**JIT有効化による明確な改善は今回の計測では確認できなかった**(無関係コンテナ稼働時に見えた「JITで1.7Gbps台まで改善」という結果も、ノイズの影響を含んでいた可能性がある)。
- **今後は計測前に`docker ps -a`で無関係なコンテナの有無を確認し、停止してから計測する運用を徹底する。** 過去の`result-*.md`(`result-20260705.md`、`result-20260705-2.md`等)の数値についても、同様のノイズが混入していた可能性があり、額面通りの比較には注意が必要。

停止した無関係コンテナ(`router`、`container-a`、`sc-router`)は、この計測のために`docker stop`しただけで削除はしていない。他のセッション/作業で必要であれば再開できる。

## 追記: XDP対応前(`performance-up-thread`ブランチ)のベースライン計測

ここまでのAF_XDP write化(`performance-up-thread-xdp`ブランチ)との比較のため、XDPを一切使わない`performance-up-thread`ブランチ(read/writeともAF_PACKET、writeのみ`parallel`スレッドへオフロード。FFI/XDP不要)に切り替えて同条件で計測した。

### 計測環境・方法

- 無関係なコンテナ(`router`、`container-a`、`sc-router`)は前節から引き続き停止したまま。本プロジェクトの4コンテナのみ稼働。
- `git checkout performance-up-thread`後、`docker compose build router && docker compose up -d --force-recreate router`でコンテナを作り直した。
- 2プロセス構成(`start_eth0.php`+`start_eth1.php`)+JIT有効で起動:
  ```bash
  docker compose exec -T router bash -c "
  cd /router
  nohup php -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth0.php > /tmp/router-eth0.log 2>&1 &
  disown
  nohup php -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth1.php > /tmp/router-eth1.log 2>&1 &
  disown"
  ```
  (このブランチはFFI不要のため`ffi.enable=true`は付けていない)
- 疎通確認後、UDP 1回・TCP 3回(再現性確認)を標準コマンドで計測。計測後、`performance-up-thread-xdp`ブランチに戻した。

### 計測結果

| 項目 | 値 |
|---|---|
| UDP 700M×4並列(受信側) | 2.44 Gbps、loss 0.4% |
| TCP -P 2(受信側、1回目) | 2.51 Gbps、retransmit 4,996 |
| TCP -P 2(受信側、2回目) | 2.49 Gbps、retransmit 4,439 |
| TCP -P 2(受信側、3回目) | 2.44 Gbps、retransmit 5,220 |

3回とも2.44〜2.51Gbps・retransmit 4,400〜5,200程度で安定しており、無関係コンテナ停止後の他の計測と同様にばらつきは小さかった。計測後もルーターPHPプロセスはクラッシュしていない。

### AF_XDP write版(単一プロセス, JIT有効, 無関係コンテナ停止後)との比較

| 項目 | **XDP対応前(AF_PACKETのみ, 2プロセス+JIT)** | AF_XDP write版(単一プロセス+JIT, 前節) |
|---|---|---|
| TCP -P 2(受信側) | **2.44〜2.51 Gbps、retransmit 4,400〜5,200** | 1.44〜1.45 Gbps、retransmit 12〜74 |

**XDP対応前(AF_PACKETのみ、2プロセス構成)の方がスループットは高い**(2.4Gbps台 vs 1.4Gbps台)。ただし比較対象がプロセス数(2プロセス vs 単一プロセス)とretransmit傾向(数千 vs 数十)の両方で異なっており、「XDP write化そのものの効果」を単独では切り出せていない点に注意が必要。フェアな比較にするには、次のいずれかで条件を揃える必要がある。

- AF_XDP write版を2プロセス構成で計測する(現状はAF_PACKETへフォールバックするため、実質「XDP write対応前と同じ経路」の計測になってしまい意味がない)。
- XDP対応前(`performance-up-thread`)を単一プロセス(`start.php`)構成で計測し、単一プロセス同士で比較する。

### 今後の選択肢(追記3)

- 上記の「単一プロセス同士」または「2プロセス同士」でのフェアな比較を、無関係コンテナ停止済みの環境で実施する。
- TCPのretransmit傾向がAF_PACKET版(数千)とAF_XDP write版(数十)とで大きく異なる理由の調査(プロセス数の違いによるものか、write経路自体の違いによるものかの切り分け)。

## 追記: recvをAF_PACKET(socket_select)に戻し、writeのみAF_XDPにする実装と計測

直前のプロファイリングで「recv(AF_XDP経由のFFI呼び出し)が処理時間の62〜73%を占める」ことが分かったのを受け、「recv側をAF_PACKETの`socket_read`系関数(`socket_select`で待つ)に戻し、writeだけAF_XDPを使う」構成を試した。

### 実装方針

- read側: `socket_create(AF_PACKET, SOCK_RAW, ETH_P_IP)` + `socket_set_nonblock()` + `socket_select()`(タイムアウト`null`でブロック待ち)+ `socket_recv()`。busy-pollをやめ、データが来るまでブロックする構成に変更(`Router.php`の`readData()`/コンストラクタ)。
- write側: **TX専用のAF_XDPハンドル**を新設。`src/c/xdpphp.c`に`xdpphp_open_tx()`を追加し、BPFプログラムのロード/attachやxsks_map・self_ip4マップへの登録を一切行わない(RXリダイレクトを使わないため不要)。これにより、従来の「combined RX+TXハンドルをメインスレッドで開いてポインタをworkerスレッドに渡す」という単一プロセス限定の複雑な仕組みが不要になり、**writeワーカースレッドが`XskSocket::openTxOnly($deviceName)`で自分自身のTX専用ハンドルを直接開くだけ**のシンプルな実装になった(`XskSocket.php`に`openTxOnly()`を追加)。
- TX専用ハンドルのopenに失敗した場合は、そのNICだけAF_PACKETの`socket_write`にフォールバックする(複数プロセス/複数workerThreadが同じNIC+queueにTX-onlyでbindしようとした場合の挙動は未検証のため)。

### 動作確認

- 単一プロセス構成(`start.php`, JIT有効)で起動したところ、AF_XDP TX-onlyのopenは両NICとも成功(フォールバックのログは出ず)。
- プロセスの状態が`ps aux`で`Rl`(busy-loop実行中)から**`Sl`(スリープ、`socket_select`でブロック待ち)に変化**しており、意図通りbusy-pollが無くなったことを確認できた。
- alice→router→bobの疎通OK、計測後もプロセスはクラッシュしていない。

### 計測環境

- 無関係なコンテナ(`router`、`container-a`、`sc-router`)は停止したまま。本プロジェクトの4コンテナのみ稼働。
- 単一プロセス構成(`start.php`)+JIT有効。UDP 1回・TCP 3回(再現性確認)。

### 計測結果

| 項目 | 値 |
|---|---|
| UDP 700M×4並列(受信側) | 2.30 Gbps、loss 0.057% |
| TCP -P 2(受信側、1回目) | 1.35 Gbps、retransmit 1,872 |
| TCP -P 2(受信側、2回目) | 1.29 Gbps、retransmit 2,015 |
| TCP -P 2(受信側、3回目) | 1.27 Gbps、retransmit 2,015 |

### これまでの単一プロセス構成の計測との比較

| 項目 | AF_XDP read + AF_XDP write(前節、クリーンな環境) | **AF_PACKET read(select) + AF_XDP write(今回)** |
|---|---|---|
| TCP -P 2(受信側) | 1.44〜1.45 Gbps、retransmit 12〜74 | **1.27〜1.35 Gbps、retransmit 1,872〜2,015** |

### 考察

- **予想通り、recvをAF_PACKETに戻したことでTCPのretransmitが大きく悪化した**(12〜74 → 1,872〜2,015)。スループットもわずかに低下した(1.44〜1.45 → 1.27〜1.35Gbps)。事前の予想(「AF_XDP読み込みへの変更でretransmitが改善した過去の実績を失うのではないか」)が実測でも裏付けられた形になった。
- UDPは2.30Gbps・loss 0.057%と、単独の数値としては悪くない(2プロセスAF_PACKETベースライン相当の2.44Gbps/loss0.4%に近い)。ただしTCPの結果を見る限り、**「recvのFFIコストを削った分だけ速くなる」という期待した効果は再現しなかった**。recvをAF_PACKETに変えても総処理時間自体はさほど減らず(システムコールのオーバーヘッドに置き換わっただけ)、むしろAF_XDPのリングバッファが持っていたバースト吸収能力を失ったことの方が効いた、という解釈が実測とも整合する。
- 一方で、**busy-pollが無くなり`Sl`(スリープ)状態になった点は明確な副次効果**。無負荷時にCPUを100%使い続けていた問題は解消される(他プロセスとのCPU共有という観点では有利になり得る)。
- TX専用AF_XDPハンドル(`xdpphp_open_tx`)を使う設計は、単一プロセス限定だった従来のポインタ受け渡し方式より実装がシンプルになった副産物として得られた。BPFプログラム/xsks_mapを介さないため、理論上は2プロセス構成でも動く可能性がある(未検証)。

### 結論

**write側だけAF_XDP化する今回の変更は、TCPのretransmit悪化というトレードオフが実測でも確認され、狙っていた速度向上は得られなかった。** 現時点では、read側もAF_XDPのままにしておく方(本ファイル前節の「AF_XDP read + AF_XDP write」)がTCPの信頼性の面で優位という結論になる。

### 今後の選択肢(追記4)

- 今回作った`xdpphp_open_tx()`(TX専用、BPFプログラム不要)を使えば、2プロセス構成(`start_eth0.php`+`start_eth1.php`)でも各プロセスのwriteワーカーが独立してTX-onlyハンドルを開けるはず。read側はAF_XDPのまま、write側だけこの新しいTX-only方式に変えて2プロセス構成で計測する価値がある(現状の2プロセス構成はwrite側がAF_PACKETフォールバックのままなので)。
- recvのFFIコストそのものを削減する方向(1回のFFI呼び出しで複数フレームをまとめて受け取るバッチAPIをC側に追加する等)を試す。

## 追記: 2プロセス構成+JITでTX-only write化を試験

前節の「今後の選択肢」で挙げた通り、2プロセス構成(`start_eth0.php`+`start_eth1.php`)+JIT有効で、TX専用AF_XDPハンドル(`xdpphp_open_tx`/`XskSocket::openTxOnly()`)を使ったwriteを試験した。

### 実装時に見つかった不具合(3件、いずれも修正済み)

実際に2プロセス構成で動かしたところ、想定していなかった3つの不具合が連続して見つかった。

1. **`parallel`のワーカースレッド内で`STDERR`定数が未定義**。フォールバック時のログ出力(`fwrite(STDERR, ...)`)自体がFatal Errorになり、本来のエラーメッセージが握りつぶされていた。`fwrite(fopen('php://stderr', 'w'), ...)`に変更して解消。
2. **`XskSocket`の`$ownsHandle`(typed property)が初期化前に参照されFatal Error**。`open()`/`openTxOnly()`がC側のオープンに失敗して例外を投げた際、デストラクタが`$ownsHandle`未初期化のまま読もうとして二次的なFatal Errorを起こし、本来の例外メッセージを隠していた。プロパティ宣言時に`= false`のデフォルト値を与えて解消。
3. **`FFI::isNull()`にPHPの`null`を渡すとTypeError**。`xdpphp_open_tx()`がC側で失敗して`NULL`を返した場合、FFIの戻り値がCDataではなくPHPの`null`そのものになるケースがあり(`xdpphp_open()`にも同じ潜在バグがあったが、これまで実際に失敗するケースを踏んだことが無く未発見だった)、`\FFI::isNull($handle)`がTypeErrorを投げていた。`$handle === null || \FFI::isNull($handle)`に修正して解消。

これら3つが折り重なって、**最初はCの本当のエラー内容が全く見えない状態**だった。1つずつ直して初めて`xsk_socket__create(tx-only) failed on eth0 queue 0: -16 (Device or resource busy)`という実際の原因(EBUSY)が見えた。

### 判明した制約: TX専用ハンドルも(ifindex, queue)単位で排他

BPFプログラム/xsks_mapを一切使わないTX専用ハンドルであっても、**AF_XDPソケットのbind()自体は(ifindex, queue)単位で排他的**であることが実測で確認できた。2プロセス構成で両プロセスが同じNIC(例: eth0)へTX-onlyでbindしようとすると、後からbindした方が`EBUSY`で失敗する。これは「RXリダイレクトが無ければ排他制約も無いはず」という実装時の見立てが誤りだったことを意味する。

この制約に対応するため、write側にも`$handleNic`による住み分けを導入した: `$handleNic`が指定されている(2プロセス構成)場合は「自分の担当NICだけAF_XDP TX-onlyを試み、他方のNICはAF_PACKETにフォールバックする」という、従来のRX側と同じ考え方をwrite側にも適用した(`Router.php`の`start()`)。単一プロセス構成(`handleNic===null`)は従来通り全NIC分TX-onlyを試みる。

### 計測環境・結果

- 無関係なコンテナは停止したまま。2プロセス構成(`start_eth0.php`+`start_eth1.php`)+JIT有効。修正後は両プロセスともフォールバックなしで起動(ログにエラー無し)。
- UDP 1回・TCP 3回(再現性確認)。

| 項目 | 値 |
|---|---|
| UDP 700M×4並列(受信側) | 2.30 Gbps、loss 0.077% |
| TCP -P 2(受信側、1回目) | 2.32 Gbps、retransmit 8,886 |
| TCP -P 2(受信側、2回目) | 2.25 Gbps、retransmit 9,103 |
| TCP -P 2(受信側、3回目) | 2.25 Gbps、retransmit 9,144 |

計測後もルーターPHPプロセス(eth0/eth1とも)はクラッシュしていない。

### これまでの計測との比較(クリーンな環境のもののみ)

| 構成 | UDP | TCP(受信側) |
|---|---|---|
| **XDP対応前ベースライン**(AF_PACKET read/write, 2プロセス+JIT) | 2.44 Gbps, loss 0.4% | 2.44〜2.51 Gbps, retransmit 4,400〜5,200 |
| AF_XDP read + AF_XDP write(単一プロセス+JIT) | (未計測) | 1.44〜1.45 Gbps, retransmit 12〜74 |
| AF_PACKET read(select) + AF_XDP TX-only write(単一プロセス+JIT) | 2.30 Gbps, loss 0.057% | 1.27〜1.36 Gbps, retransmit 1,872〜2,015 |
| **AF_PACKET read(select) + AF_XDP TX-only write、自NICのみ(2プロセス+JIT、今回)** | 2.30 Gbps, loss 0.077% | **2.25〜2.34 Gbps, retransmit 8,886〜9,144** |

### 考察

- 2プロセス化により、単一プロセスのTX-only版(1.27〜1.36Gbps)よりスループットは大きく改善し(2.25〜2.34Gbps)、XDP対応前ベースライン(2.44〜2.51Gbps)に近い水準まで戻った。プロセスを分けてCPUを複数コアに分散させる効果は、write側の実装方式によらず大きいことが改めて確認できた。
- 一方で**retransmitはベースラインの倍近く(4,400〜5,200 → 8,886〜9,144)に悪化しており、スループットが近い水準まで戻った代償としてTCPの信頼性は明確に犠牲になっている**。この構成では書き込みの半分(自NIC宛て)がAF_XDP TX-only、残り半分(他NIC宛て)がAF_PACKETという**2つの異なる書き込み経路が1本の書き込みスレッド内で混在**しており、この非対称性自体が悪影響を及ぼしている可能性がある(未検証)。
- 総合すると、**今回試したどのAF_XDP write化のバリエーションも、「XDP対応前のシンプルなAF_PACKETベースライン(2プロセス+JIT)」をスループット・信頼性の両面で上回ることはできなかった**。唯一retransmitが顕著に改善したのは「read側もAF_XDPのままにする」構成(単一プロセス、retransmit 12〜74)だが、これはスループットを大きく犠牲にしている。

### 結論

**write側をAF_XDP化する一連の実験を通じて、「XDP対応前のAF_PACKETのみ・2プロセス+JIT構成」を明確に上回る組み合わせは見つからなかった。** 現状のベストな構成は依然として「read/writeともAF_PACKET、2プロセス+JIT」(`performance-up-thread`ブランチ相当)であり、AF_XDPは「read側に使うとTCP retransmitが大幅に減る」という特性はあるものの、スループットとのトレードオフが大きく、現行の実装(1フレームずつFFI越しに処理する設計)のままでは総合的な優位性を示せていない。

### 今後の選択肢(追記5)

- 今回混在が疑われた「1本の書き込みスレッド内でのAF_XDP/AF_PACKET混在」の影響を切り分けるため、自NIC・他NICとも同じ経路(全部AF_XDP or 全部AF_PACKET)に揃えた2プロセス構成での比較を行う。
- recvのFFIコストそのものを削減する方向(1回のFFI呼び出しで複数フレームをまとめて受け取るバッチAPIをC側に追加する等)を試す。
- 今回見つかった3つのFFI/parallel関連の不具合(STDERR未定義、typed propertyの未初期化アクセス、FFI::isNullへのnull渡し)は、今後AF_XDP関連のコードを触る際に再発しやすいポイントとして留意する。

## 追記: 2プロセス+JITで「recv/writeともAF_XDP」を試験 → これまでで最良の結果

前節の考察を踏まえ、「自NIC宛て書き込みもAF_PACKETへフォールバックせず、read側と同じAF_XDPハンドルのTX ringをそのまま使う」構成(＝2〜3個前の単一プロセス限定だった設計を2プロセス構成にも一般化したもの)を2プロセス+JITで試した。

### 実装内容

- read側: `XskSocket::open()`によるAF_XDP RX+TX兼用ハンドルに戻した(`$readSockets`のAF_PACKET+`socket_select`版から復元)。`readData()`も busy-poll の`recvFrame()`ループに戻した。
- write側: 新設した`xdpphp_open_tx()`(TX専用、独立bind)は使わず、**read側で既に開いている自NIC分のRX+TXハンドルのアドレスを`getHandleAddress()`で取得し、`parallel`ワーカースレッドへ渡して`XskSocket::fromHandleAddress()`で再構築、そのTX ringをそのまま使う**方式に戻した(2〜3個前の単一プロセス限定だった実装と同じ仕組み)。
- 従来「単一プロセス構成(`handleNic===null`)のみ」に限定していた条件分岐(`$useXdpWrite`)を撤廃し、**`$this->sockets`に入っているNIC(=自プロセスが担当するNIC)は無条件でAF_XDP writeを使う**よう単純化した。他プロセスが担当するNIC(`$this->sockets`に無い)は自動的にAF_PACKETフォールバックになる。
- 理由: AF_XDPのbind()は(ifindex, queue)単位で排他的なので、自NICは「read側が既に持っているハンドル」を共有すれば新規bindが要らずEBUSYを回避できる。他NICは他プロセスが排他的にbind済みのため、TX-onlyであっても新規bindは必ずEBUSYになる(前節で実測済み)。よって「自NICは共有TX、他NICはAF_PACKET」が唯一の整合する設計。

### 動作確認

- 2プロセス構成(`start_eth0.php`+`start_eth1.php`)+JIT有効で起動、フォールバックログ無し、`ps aux`は`Rl`(AF_XDP recvのbusy-poll)。
- alice→router→bobの疎通OK。

### 計測結果(無関係なコンテナは停止したまま)

| 項目 | 値 |
|---|---|
| UDP 700M×4並列(受信側) | **2.78 Gbps、loss 0.13%** |
| TCP -P 2(受信側、1回目) | 2.43 Gbps、retransmit 170 |
| TCP -P 2(受信側、2回目) | 2.58 Gbps、retransmit 28 |
| TCP -P 2(受信側、3回目) | 2.49 Gbps、retransmit 4 |

計測後もルーターPHPプロセス(eth0/eth1とも)はクラッシュしていない。

### これまでの計測との比較(クリーンな環境のもののみ)

| 構成 | UDP | TCP(受信側) |
|---|---|---|
| XDP対応前ベースライン(AF_PACKET read/write, 2プロセス+JIT) | 2.44 Gbps, loss 0.4% | 2.44〜2.51 Gbps, retransmit 4,400〜5,200 |
| AF_XDP read + AF_XDP write(単一プロセス+JIT) | (未計測) | 1.44〜1.45 Gbps, retransmit 12〜74 |
| AF_PACKET read(select) + AF_XDP TX-only write(単一プロセス+JIT) | 2.30 Gbps, loss 0.057% | 1.27〜1.36 Gbps, retransmit 1,872〜2,015 |
| AF_PACKET read(select) + AF_XDP TX-only write、自NICのみ(2プロセス+JIT) | 2.30 Gbps, loss 0.077% | 2.25〜2.34 Gbps, retransmit 8,886〜9,144 |
| **AF_XDP read + AF_XDP write(TXは自NIC分を共有)、2プロセス+JIT(今回)** | **2.78 Gbps, loss 0.13%** | **2.43〜2.58 Gbps, retransmit 4〜170** |

### 考察

- **これまでの全構成の中で、スループット・信頼性(retransmit)の両方でXDP対応前ベースラインを明確に上回った初めての結果**。UDPはベースラインの2.44Gbpsに対し2.78Gbpsと+14%、TCPはスループットが同等以上(2.43〜2.58 vs 2.44〜2.51Gbps)でありながら、retransmitはベースラインの1/30〜1/1000程度(4,400〜5,200 → 4〜170)まで激減した。
- 前節までの「TX-onlyでEBUSYを踏んで自NIC/他NICでAF_XDP/AF_PACKETが混在し、retransmitが悪化した」結果と比較すると、**「自NICの書き込みはread側と同じハンドル(=同じUMEM、同じ設計思想)を共有する」ことが重要**だったことが分かる。TX専用の別ハンドルを新設するアプローチ(`xdpphp_open_tx`)は、動作はするものの今回のような良い結果には繋がらなかった。
- 結局のところ、**「2〜3個前の実験で単一プロセス限定にしていたポインタ共有方式を、実は2プロセス構成にも一般化できた」というのが今回の核心**。当時`$useXdpWrite`の条件を`handleNic===null`に限定していたのは早計で、`$this->sockets`に入っているNIC単位で判定すれば2プロセス構成でも自然に成立する設計だった。

### 結論

**「read=AF_XDP、write=AF_XDPだが自NIC分は同一ハンドルのTX ringを共有・他NICはAF_PACKETフォールバック」という構成を2プロセス+JITで動かした結果が、本日の一連の実験の中で最も優れていた。** XDP対応前のシンプルなAF_PACKETベースラインを、スループット・信頼性の両面で上回ることに初めて成功した。

### 今後の選択肢(追記6)

- この構成を正式なベスト構成の候補として、RPS有効時や複数回・長時間の計測で再現性を確認する。
- なぜこの構成だけ良い結果になったのか(UMEM/ハンドル共有の有無が具体的にどう効いているか)を、`xdpphp_send`のバックプレッシャーdrop率などを計測して裏付ける。
- write側のworkerCountを増やす場合は、以前の実験(`workerCount=2`でパケット順序が崩れてretransmit急増)の教訓を踏まえ、フロー単位の振り分けを併せて検討する。
