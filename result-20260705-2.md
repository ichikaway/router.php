# Hybrid構成(read=AF_XDP / write=AF_PACKET+スレッド)の計測結果 (2026-07-05)

`performance-up-thread-xdp` ブランチ。`result-20260705.md`(read/writeともAF_XDP、単一スレッド)からの変更として、**writeは既存のAF_PACKETソケット+`parallel`スレッドオフロードに戻し、readのみAF_XDPを使う構成**に変更して再計測した。

## 実装内容の変更点

- `src/Router.php`:
  - `use parallel\Runtime; use parallel\Channel;`と`workerCount`プロパティを復活。
  - `start()`に、ワーカースレッド(`parallel\Runtime`)ごとに`socket_create(AF_PACKET, SOCK_RAW, ETH_P_IP)`のwrite専用ソケットを作り`Channel`経由でフレームを受け取って`socket_write`する元の実装を復元。
  - 転送処理の最後は`$this->sockets[...]->sendFrame(...)`(AF_XDP送信)ではなく、`$chan[$cnt]->send([$dstPkt, $writeDeviceName])`(Channel経由でAF_PACKET書き込みスレッドへ)に変更。
  - `readData()`/コンストラクタ側(AF_XDPでの読み込み)は変更なし。
- `src/Xdp/XskSocket.php`: 送信(`sendFrame`)とその CDEF宣言(`xdpphp_send`)を削除。RX専用のクラスに縮小。
- `src/c/xdpphp.c`: TXリング・completion ring・TX用フレームfree-list・`xdpphp_send()`関数を削除。UMEMもRX用フレーム(2048枚)のみに縮小(TX用に確保していた分を廃止)。`xsk_socket__create()`の`tx`引数は`NULL`、`xsk_socket_config.tx_size`は`0`にしてRX専用ソケットとして構成。

AF_XDP側で送信を行わないため、「送信フレームが自分のRXリングへ回り込む」ことを心配する必要もなく、AF_PACKETの書き込み専用ソケットとXDPのredirect(受信専用)は競合しない(XDPは受信パス、AF_PACKET書き込みは送信パスのみを使うため独立している)。

## 計測環境・方法

- docker-compose上のalice(10.0.0.10) → router(eth0=10.0.0.250/eth1=10.0.1.250, privileged) → bob(10.0.1.10)。net1/net2はdocker bridge経由のveth。alice/bobは`ethtool -K eth0 rx off tx off tso off gro off`適用済み。
- 比較対象は3種類:
  1. **AF_PACKET版(ベースライン)**: `performance-up-thread`ブランチ。read/writeともAF_PACKET、writeのみ`parallel`スレッドへオフロード。
  2. **AF_XDP単独版**: `result-20260705.md`で計測したread/writeともAF_XDP、単一スレッド版。
  3. **kernelのip_forward**: PHPルーターを停止し、`ip link set dev eth0/eth1 xdp off`でXDPプログラムを完全に外したうえで、routerコンテナの`net.ipv4.ip_forward`を`1`にし、カーネルの経路制御のみでalice⇔bob間を転送させたもの。計測後は`ip_forward=0`に戻しPHPルーター(hybrid版)を再起動して原状復帰した。
- 実行コマンド:
  ```
  # UDP (alice → bob, 4並列)
  iperf3 -c 10.0.1.10 -u -b 700M -P 4 -t 10

  # TCP (alice → bob, 2並列)
  iperf3 -c 10.0.1.10 -P 2 -t 10
  ```
- いずれも`docker compose exec`で対象ブランチをbuild/起動し直し、同一のiperf3コマンドで直接比較した。

## 計測結果

| 項目 | AF_PACKET版(既存,マルチスレッド) | AF_XDP単独版(単一スレッド) | **Hybrid版(read=AF_XDP/write=AF_PACKET+スレッド)** | kernel ip_forward |
|---|---|---|---|---|
| UDP 700M×4並列(受信側) | 2.65 Gbps、loss 1.4% | 1.56 Gbps、loss 44% | **2.53 Gbps、loss 3.1%** | 送信側が1.91 Gbpsで頭打ち(要求2.8Gbps)、loss ~0.0008% |
| TCP -P 2(受信側) | 1.45 Gbps、retransmit 1,779 | 1.03 Gbps、retransmit 105 | **1.65 Gbps、retransmit 12** | 送信側は3.12 Gbpsまで出るが受信側は同量を受け切るのに約2倍(20秒)かかっており、実効スループットは約1.56 Gbps相当 |

補足:
- kernel ip_forwardのUDP計測では、受信側のパケットロスではなく**送信側(alice)自体の送出レートが1.91Gbpsで頭打ち**になった。他の構成(AF_PACKET/AF_XDP/Hybrid)はいずれも送信側は要求通り2.8Gbpsを維持できており、受信側でロスが出る形だったのと対照的。
- kernel ip_forwardのTCP計測では、iperf3クライアント側は10秒で送信完了と報告する一方、サーバー側(bob)の受信完了報告は0.00-20.05秒とほぼ倍の時間を要しており、バッファブロート(送信側が実際の転送能力を超えて先行してデータを送り込み、受信側でのドレインに時間がかかる現象)が疑われる。見かけ上の送信側スループット(3.12Gbps)は実態を反映していないため、受信側の実測(約1.56Gbps相当、20秒換算)を実効値として扱った。
- このveth+bridge多段構成でのkernel ip_forward自体の頭打ちの原因(単一コアでのsoftirq処理、netfilter/conntrackのオーバーヘッド、veth-bridge間の複数ホップなど)は未調査であり、上記は観測事実の記録であって原因を特定したものではない。

## 考察

- Hybrid版(read=AF_XDP、write=既存のAF_PACKET+スレッド)は、`result-20260705.md`で報告した「read/writeともAF_XDP・単一スレッド」版から大幅に改善し、UDPはAF_PACKETベースライン(2.65Gbps)に近い2.53Gbps・loss 3.1%まで回復、**TCPはAF_PACKETベースライン(1.45Gbps)・kernel ip_forward(実効約1.56Gbps)のいずれも上回る1.65Gbps**という結果になった。
- 単一スレッドAF_XDP版でCPU 100%(1コア)がボトルネックになっていた問題は、write処理を別スレッド(別コア)に戻したことで緩和され、readをAF_XDP化した分のオーバーヘッド削減効果も同時に効いていると考えられる。
- この検証環境(veth、copyモード)に限って言えば、「read側だけをAF_XDPに置き換え、write側は元のAF_PACKETスレッドオフロードのまま残す」という今回のhybrid構成が、これまで試した中で最も高いスループットを示した。

## 追記: start_eth0.php / start_eth1.phpによる2プロセス構成

Hybrid版のまま、NICごとにプロセスを分ける`start_eth0.php`/`start_eth1.php`の構成でも動かせるか検証した。

### 発覚した問題と修正

`DeviceInfo::getDevice()`は引数の有無に関わらず`eth[0-9]`にマッチする全NIC(eth0・eth1両方)を自動検出するため、`start_eth0.php`と`start_eth1.php`をそれぞれ別プロセスとして起動すると、**両プロセスがeth0とeth1の両方に対してAF_XDP受信ソケット(XskSocket)を作ろうとしてしまう**問題があった。AF_PACKETでは複数プロセスが同じNICに読み取りソケットを重複して持っても実害はない(未使用な方は単に読み捨てられるだけ)が、AF_XDPは同じ(インターフェース, queue)に対して後から`xdpphp_open()`したプロセスがXDPプログラムとXSKMAPを丸ごと差し替えてしまうため、先に起動した方のAF_XDP受信が(エラーも出ずに)静かに死ぬ。

`src/Router.php`を以下のように修正して対応した:
- コンストラクタ: AF_XDP受信ソケット(`XskSocket`)の生成を`$handleNic === null || $Device->getDeviceName() === $handleNic`の場合のみに限定(write用のAF_PACKETソケットは元々全NIC分必要なため、`$devices`配列自体は常に全NIC分保持する)。
- `start()`: write用ソケット一覧の取得元を、handleNicで絞られた`$this->sockets`ではなく、常に全NICを持つ`$this->devices`に変更(`$nicList = array_keys($this->devices);`)。

修正後、`bpftool net show`でeth0とeth1にそれぞれ別のprog id(3503, 3504)がアタッチされ、競合していないことを確認した。

### 計測結果(2プロセス版)

| 項目 | 単一プロセスHybrid版 | **2プロセス(start_eth0.php + start_eth1.php)Hybrid版** |
|---|---|---|
| UDP 700M×4並列(受信側) | 2.53 Gbps、loss 3.1% | **2.60 Gbps、loss 0.6%** |
| TCP -P 2(受信側) | 1.65 Gbps、retransmit 12 | **2.51 Gbps、retransmit 4,795** |

2プロセス化により、TCPが1.65Gbpsから2.51Gbpsへ大幅に向上した。read処理自体を2プロセス(≒2コア)に分散できたことに加え、各プロセスがそれぞれ独自のwriteスレッドを持つため、全体としてより多くのCPUコアを使えるようになったことが要因と考えられる。今回試した構成の中で最も高いスループットとなった。ただしTCPのretransmit数は単一プロセス版(12)より大幅に増えており(4,795)、スループットは上がった一方でロス/再送は増えているというトレードオフがある点は留意する。

検証後は、この2プロセス構成(`start_eth0.php`+`start_eth1.php`)を起動したままにしている。

## 追記: 2プロセス構成でのJITコンパイル有無比較

2プロセス(start_eth0.php + start_eth1.php)Hybrid版はそのままに、PHPのJITを有効にして再計測した。

```
php -d ffi.enable=true -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth0.php
php -d ffi.enable=true -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth1.php
```

### 計測結果(JITなし vs JITあり)

| 項目 | JITなし | **JITあり** |
|---|---|---|
| UDP 700M×4並列(受信側) | 2.60 Gbps、loss 0.6% | **2.67 Gbps、loss 0.19%** |
| TCP -P 2(受信側) | 2.51 Gbps、retransmit 4,795 | **2.59 Gbps、retransmit 20** |

スループット自体もわずかに向上しているが、それ以上に**TCPのretransmitが4,795→20へ大幅に減少**した点が顕著。イーサネット/IPヘッダのパース、ルーティングテーブル検索、ARPキャッシュ参照などRouter.phpのホットループがJITでコンパイルされ処理が追いつくようになったことで、ロス・再送が大きく減ったと考えられる。

検証後は、この2プロセス+JIT有効の状態を起動したままにしている。

## 追記: AF_PACKET版(XDP不使用)2プロセス+JIT、およびkernel ip_forward=1の再計測

比較のため、`performance-up-thread`ブランチ(read/writeともAF_PACKET、XDP不使用)でも同じ2プロセス構成(`start_eth0.php`+`start_eth1.php`)にJITを有効にして計測した。あわせて、同ブランチ上でPHPルーターを停止し`net.ipv4.ip_forward=1`にしたkernel forwardingも再計測した(前回`result-20260705-2.md`本文の計測はAF_XDP版ブランチ上で行ったもの。今回はAF_PACKET版ブランチ上での値)。

```
php -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth0.php
php -d opcache.enable=1 -d opcache.enable_cli=1 -d opcache.jit_buffer_size=100M -d opcache.jit=1255 src/start_eth1.php
```

### 計測結果

| 項目 | AF_PACKET 2プロセス+JIT | AF_XDP hybrid 2プロセス+JIT(前掲) | kernel ip_forward=1(このブランチ上で再計測) |
|---|---|---|---|
| UDP 700M×4並列(受信側) | 2.52 Gbps、loss 0.57% | 2.67 Gbps、loss 0.19% | 1.89 Gbps、loss 0%(送信側自体が1.90Gbpsで頭打ち) |
| TCP -P 2(受信側) | 2.45 Gbps、retransmit 5,155 | 2.59 Gbps、retransmit 20 | 3.16 Gbps、retransmit 287 |

### 考察

- **AF_PACKET版はJITを有効にしてもretransmitが5,155と高止まりしたまま**であり、AF_XDP hybrid+JIT版のretransmit 20とは対照的だった。前回「JITでretransmitが激減した」効果は、JIT単体ではなく「AF_XDPによるskb確保・フルスタック通過の回避」と「JIT」の組み合わせによるものである可能性が高い。
- kernel ip_forward=1のTCPは今回3.16Gbpsとなり、`result-20260705-2.md`本文で報告したバッファブロート(送信側の見かけ上の数値が実態と乖離する現象)は今回は見られず、素直に高い実効値が出た。この値はいずれのPHPルーター構成(AF_PACKET/AF_XDP hybrid、JIT有無問わず)よりも高く、**カーネル単体転送の性能にはまだ届いていない**ことを示している。
- UDPのkernel ip_forward=1は今回も送信側自体が1.90Gbps程度で頭打ちになる現象を再現した(前回計測と同様)。

## 追記: kernel ip_forwardが1.9Gbpsに頭打ちする原因調査

前節までで繰り返し観測された「kernel ip_forward=1のUDPは送信側自体が1.9Gbps付近で頭打ちになる」現象について原因を調査した。

### 調査手順

1. routerコンテナのCPU数を確認: `nproc` → **24**
2. vethのキュー構成を確認:
   ```
   ls /sys/class/net/eth0/queues/
   → rx-0 tx-0   (1本のみ。物理NICのRSSのような複数キュー・複数IRQでの分散が最初から存在しない)
   ```
3. netfilter/conntrackの影響を確認: `iptables -L -n -v`(filter/nat とも空、DOCKER_OUTPUT/POSTROUTINGは埋め込みDNS用のみ)、`/proc/sys/net/netfilter/nf_conntrack_count` → `0`。ここはボトルネックではないと判断。
4. RPS(Receive Packet Steering)の設定を確認:
   ```
   cat /sys/class/net/eth0/queues/rx-0/rps_cpus
   → 000000   (無効)
   ```
5. `ip_forward=1`でUDPベンチマークを実行しながら、`/proc/stat`を前後でサンプリングしCPUごとの使用率を算出。結果、**24コア中1コア(cpu13)だけが87%使用率(うち94%がsoftirq)、他23コアはほぼアイドル**という偏りを確認。

### 検証: RPSを有効化して再計測

原因の裏付けとして、`rps_cpus`に全24コア分のビットマスクを設定し同じベンチマークを再実行した。

```bash
# 全24コアをRPS対象にする(24bit = 6桁の16進 f)
echo ffffff > /sys/class/net/eth0/queues/rx-0/rps_cpus
echo ffffff > /sys/class/net/eth1/queues/rx-0/rps_cpus

# 確認
cat /sys/class/net/eth0/queues/rx-0/rps_cpus
```

| 項目 | RPS無効(元の状態) | **RPS有効** |
|---|---|---|
| UDP 700M×4並列 | 送信側1.91Gbpsで頭打ち、loss 0% | **送信側2.80Gbps(要求通り)、受信側2.79Gbps、loss 0%** |
| TCP -P 2 | 3.16 Gbps | **6.88 Gbps** |

RPS有効時に同様に`/proc/stat`を確認したところ、cpu11/13/18/22/23など複数コアが30〜77%使用率に分散しており、単一コア張り付きが解消されていることを確認した。

### 結論

vethはハードウェア的なマルチキュー/RSSを持たないソフトウェアデバイスのため、RPSを設定しない限り受信側のsoftirq処理(NAPI poll、ブリッジ/ルーティング処理)が単一CPUコアに固定されてしまう。ホストに24コアあってもこの1コアの処理能力(このワークロードでは概ね1.9Gbps相当)がスループットの上限になっていた。RPSを有効にするだけでUDP/TCPともに大幅に改善し、TCPは6.88Gbpsとこれまで計測したどのPHPルーター構成よりも高速になった。**つまり、これまでの「kernelでも約1.9〜3.16Gbps止まり」という比較は、RPS未設定という見落としのために本来のカーネル転送性能を過小評価していた可能性が高い。**

検証後は`rps_cpus`を`0`に戻し、`ip_forward`も`0`に戻して2プロセスHybrid+JIT構成のルーターを再起動している。

### rps_cpusの設定方法(参考)

- 設定ファイル: `/sys/class/net/<デバイス名>/queues/rx-<キュー番号>/rps_cpus`(vethはキューが1本のみなので`rx-0`)
- 値は対象にしたいCPUのビットマスクを16進数で書き込む。例:
  - `echo 1 > .../rps_cpus` → CPU0のみ
  - `echo 3 > .../rps_cpus` → CPU0,1
  - `echo ffffff > .../rps_cpus` → 24コア分(bit0〜23)全て
- 反映はコンテナ内(ネットワーク名前空間内)の設定のみで完結し、再起動すると失われる一時的な設定(永続化するにはentrypoint.sh等での自動設定が必要)。
- 併せて`/proc/sys/net/core/rps_sock_flow_entries`と各キューの`rps_flow_cnt`を設定するとRFS(Receive Flow Steering、フロー単位でのCPU固定)も有効化できるが、今回はrps_cpusのみで十分な改善が確認できたため未検証。

## 追記: RPS有効化した状態での3構成の再比較

前節の追記で提起した「フェアな比較としてはRPS込みでの再計測が望ましい」を受け、`rps_cpus`を常に`ffffff`(24コア全て)に設定した状態で、AF_PACKET版2プロセス+JIT、AF_XDP hybrid版2プロセス+JIT、kernel ip_forward=1の3つを同条件で再計測した。手順は`howto-measure.md`のRPS設定手順に沿う(コンテナ再作成のたびに`rps_cpus`は`0`に戻るため、ブランチを切り替えるたびに設定し直している)。

### 計測結果(RPS有効)

| 項目 | AF_PACKET 2プロセス+JIT+RPS | AF_XDP hybrid 2プロセス+JIT+RPS | kernel ip_forward+RPS |
|---|---|---|---|
| UDP 700M×4並列(受信側) | 2.11 Gbps、**loss 19%** | 2.65 Gbps、loss 0.21% | **2.79 Gbps、loss 0%** |
| TCP -P 2(受信側) | 2.47 Gbps、retransmit 10,097 | 2.60 Gbps、retransmit 2,070 | **6.78 Gbps**、retransmit 975 |

(参考: RPS無効時は本文記載の通り AF_PACKET UDP 2.52Gbps/loss 0.57%・TCP 2.45Gbps/retrans 5,155、hybrid UDP 2.67Gbps/loss 0.19%・TCP 2.59Gbps/retrans 20、kernel UDP 1.89Gbps/TCP 3.16Gbps)

### 考察: RPSの効果は経路によって正反対になる

3構成でRPSの効果がまったく異なった。

- **kernel ip_forward**: 前節同様、劇的に改善(UDP 1.89→2.79Gbps、TCP 3.16→6.78Gbps)。ip_forwardは通常のネットワークスタック(`netif_receive_skb`)を経由するため、RPSによる複数CPUへの分散がそのまま効く。
- **AF_XDP hybrid**: ほぼ変化なし(UDP 2.67→2.65Gbps、TCP 2.59→2.60Gbps)。XDPの`bpf_redirect_map()`はRPSによるCPU振り分けの判定より手前、すなわちskbが生成される前の段階で完結するため、転送対象トラフィックの経路はそもそもRPSの影響を受けない。TCPのretransmitは20→2,070へ増えているが、これは自ノード宛てトラフィック(ARP/ping等、RPSの影響を受ける経路)側の揺らぎである可能性がある(未検証)。
- **AF_PACKET**: 逆に悪化した(UDP loss 0.57%→19%、TCP retransmit 5,155→10,097)。RPSにより複数CPUが同時に同じAF_PACKETソケットの受信キューへパケットを配送するようになり、ソケット内部のロック競合やパケット到着順序の乱れが増えたためと推測される(要検証、`perf`等でのロック競合プロファイリングは今回未実施)。

**結論: 「RPSを設定すれば速くなる」ではなく、経路(kernelスタックを通るか、XDPでバイパスするか、AF_PACKETソケットでタップするか)によって効果の方向性自体が変わる。** 今回の検証環境では、AF_XDP hybrid構成がRPSの有無に関わらず安定して動く一方、AF_PACKET構成はRPSを設定するとむしろ悪化するため、**AF_PACKETを使う場合はRPSを設定しない方が良い**という実務上の示唆が得られた。kernel ip_forwardは常にRPS込みで比較するのがフェア。

検証後は、この計測に使った`rps_cpus=ffffff`設定のまま、AF_XDP hybrid+JIT構成の2プロセスを起動したままにしている(今後の計測は原則RPSを設定した状態で行う方針のため)。

## 今後の選択肢

- write側もAF_XDPで並列化する(`xsk_socket__create_shared`によるUMEM共有、plan.mdのOption A)を試し、2プロセスHybrid版をさらに上回れるか検証する。
- 実NIC(zero-copy対応ドライバ)環境での再検証。
- AF_PACKET版でJITを有効にしてもretransmitが高止まりする理由の調査(AF_XDP+JITの組み合わせでのみ大幅改善した理由の裏付け)。
- AF_PACKETがRPSで悪化する理由(ソケット受信キューのロック競合等)の`perf`によるプロファイリング。
- PHPルーター側(AF_PACKET/AF_XDPいずれも)のwrite用veth(eth0/eth1)についてもRPS/RFSが有効かどうかの確認。
