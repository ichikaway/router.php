# Hybrid構成(read=AF_XDP / write=AF_PACKET+スレッド)の計測結果 (2026-07-05)

`performance-up-thread-xdp` ブランチ。`result-20260705.md`(read/writeともAF_XDP、単一スレッド)からの変更として、**writeは既存のAF_PACKETソケット+`parallel`スレッドオフロードに戻し、readのみAF_XDPを使う構成**に変更して再計測した。

## 実装内容の変更点

- `src/Router.php`:
  - `use parallel\Runtime; use parallel\Channel;`と`workerCount`プロパティを復活。
  - `start()`に、ワーカースレッド(`parallel\Runtime`)ごとに`socket_create(AF_PACKET, SOCK_RAW, ETH_P_IP)`のwrite専用ソケットを作り`Channel`経由でフレームを受け取って`socket_write`する元の実装を復元。
  - 転送処理の最後は`$this->sockets[...]->sendFrame(...)`(AF_XDP送信)ではなく、`$chan[$cnt]->send([$dstPkt, $writeDeviceName])`(Channel経由でAF_PACKET書き込みスレッドへ)に変更。
  - `readData()`/コンストラクタ側(AF_XDPでの読み込み)は変更なし。
- `src/Xdp/XskSocket.php`: 送信(`sendFrame`)とその CDEF宣言(`xdpphp_send`)を削除。RX専用のクラスに縮小。
- `src/c/xdpphp.c`: TXリング・completion ring・TX用フレームfree-list・`xdpphp_send()`関数を削除。UMEMもRX用フレーム(2048枚)のみに縮小(TX用に確保していた分を廃止)。`xsk_socket__create()`の`tx`引数は`NULL`、`xsk_socket_config.tx_size`は`0`にしてRX専用ソケットとして構成。

AF_XDP側で送信を行わないため、「送信フレームが自分のRXリングへ回り込む」ことを心配する必要もなく、AF_PACKETの書き込み専用ソケットとXDPのredirect(受信専用)は競合しない(XDPは受信パス、AF_PACKET書き込みは送信パスのみを使うため独立している)。

## 計測環境・方法

- docker-compose上のalice(10.0.0.10) → router(eth0=10.0.0.250/eth1=10.0.1.250, privileged) → bob(10.0.1.10)。net1/net2はdocker bridge経由のveth。alice/bobは`ethtool -K eth0 rx off tx off tso off gro off`適用済み。
- 比較対象は3種類:
  1. **AF_PACKET版(ベースライン)**: `performance-up-thread`ブランチ。read/writeともAF_PACKET、writeのみ`parallel`スレッドへオフロード。
  2. **AF_XDP単独版**: `result-20260705.md`で計測したread/writeともAF_XDP、単一スレッド版。
  3. **kernelのip_forward**: PHPルーターを停止し、`ip link set dev eth0/eth1 xdp off`でXDPプログラムを完全に外したうえで、routerコンテナの`net.ipv4.ip_forward`を`1`にし、カーネルの経路制御のみでalice⇔bob間を転送させたもの。計測後は`ip_forward=0`に戻しPHPルーター(hybrid版)を再起動して原状復帰した。
- 実行コマンド:
  ```
  # UDP (alice → bob, 4並列)
  iperf3 -c 10.0.1.10 -u -b 700M -P 4 -t 10

  # TCP (alice → bob, 2並列)
  iperf3 -c 10.0.1.10 -P 2 -t 10
  ```
- いずれも`docker compose exec`で対象ブランチをbuild/起動し直し、同一のiperf3コマンドで直接比較した。

## 計測結果

| 項目 | AF_PACKET版(既存,マルチスレッド) | AF_XDP単独版(単一スレッド) | **Hybrid版(read=AF_XDP/write=AF_PACKET+スレッド)** | kernel ip_forward |
|---|---|---|---|---|
| UDP 700M×4並列(受信側) | 2.65 Gbps、loss 1.4% | 1.56 Gbps、loss 44% | **2.53 Gbps、loss 3.1%** | 送信側が1.91 Gbpsで頭打ち(要求2.8Gbps)、loss ~0.0008% |
| TCP -P 2(受信側) | 1.45 Gbps、retransmit 1,779 | 1.03 Gbps、retransmit 105 | **1.65 Gbps、retransmit 12** | 送信側は3.12 Gbpsまで出るが受信側は同量を受け切るのに約2倍(20秒)かかっており、実効スループットは約1.56 Gbps相当 |

補足:
- kernel ip_forwardのUDP計測では、受信側のパケットロスではなく**送信側(alice)自体の送出レートが1.91Gbpsで頭打ち**になった。他の構成(AF_PACKET/AF_XDP/Hybrid)はいずれも送信側は要求通り2.8Gbpsを維持できており、受信側でロスが出る形だったのと対照的。
- kernel ip_forwardのTCP計測では、iperf3クライアント側は10秒で送信完了と報告する一方、サーバー側(bob)の受信完了報告は0.00-20.05秒とほぼ倍の時間を要しており、バッファブロート(送信側が実際の転送能力を超えて先行してデータを送り込み、受信側でのドレインに時間がかかる現象)が疑われる。見かけ上の送信側スループット(3.12Gbps)は実態を反映していないため、受信側の実測(約1.56Gbps相当、20秒換算)を実効値として扱った。
- このveth+bridge多段構成でのkernel ip_forward自体の頭打ちの原因(単一コアでのsoftirq処理、netfilter/conntrackのオーバーヘッド、veth-bridge間の複数ホップなど)は未調査であり、上記は観測事実の記録であって原因を特定したものではない。

## 考察

- Hybrid版(read=AF_XDP、write=既存のAF_PACKET+スレッド)は、`result-20260705.md`で報告した「read/writeともAF_XDP・単一スレッド」版から大幅に改善し、UDPはAF_PACKETベースライン(2.65Gbps)に近い2.53Gbps・loss 3.1%まで回復、**TCPはAF_PACKETベースライン(1.45Gbps)・kernel ip_forward(実効約1.56Gbps)のいずれも上回る1.65Gbps**という結果になった。
- 単一スレッドAF_XDP版でCPU 100%(1コア)がボトルネックになっていた問題は、write処理を別スレッド(別コア)に戻したことで緩和され、readをAF_XDP化した分のオーバーヘッド削減効果も同時に効いていると考えられる。
- この検証環境(veth、copyモード)に限って言えば、「read側だけをAF_XDPに置き換え、write側は元のAF_PACKETスレッドオフロードのまま残す」という今回のhybrid構成が、これまで試した中で最も高いスループットを示した。

## 追記: start_eth0.php / start_eth1.phpによる2プロセス構成

Hybrid版のまま、NICごとにプロセスを分ける`start_eth0.php`/`start_eth1.php`の構成でも動かせるか検証した。

### 発覚した問題と修正

`DeviceInfo::getDevice()`は引数の有無に関わらず`eth[0-9]`にマッチする全NIC(eth0・eth1両方)を自動検出するため、`start_eth0.php`と`start_eth1.php`をそれぞれ別プロセスとして起動すると、**両プロセスがeth0とeth1の両方に対してAF_XDP受信ソケット(XskSocket)を作ろうとしてしまう**問題があった。AF_PACKETでは複数プロセスが同じNICに読み取りソケットを重複して持っても実害はない(未使用な方は単に読み捨てられるだけ)が、AF_XDPは同じ(インターフェース, queue)に対して後から`xdpphp_open()`したプロセスがXDPプログラムとXSKMAPを丸ごと差し替えてしまうため、先に起動した方のAF_XDP受信が(エラーも出ずに)静かに死ぬ。

`src/Router.php`を以下のように修正して対応した:
- コンストラクタ: AF_XDP受信ソケット(`XskSocket`)の生成を`$handleNic === null || $Device->getDeviceName() === $handleNic`の場合のみに限定(write用のAF_PACKETソケットは元々全NIC分必要なため、`$devices`配列自体は常に全NIC分保持する)。
- `start()`: write用ソケット一覧の取得元を、handleNicで絞られた`$this->sockets`ではなく、常に全NICを持つ`$this->devices`に変更(`$nicList = array_keys($this->devices);`)。

修正後、`bpftool net show`でeth0とeth1にそれぞれ別のprog id(3503, 3504)がアタッチされ、競合していないことを確認した。

### 計測結果(2プロセス版)

| 項目 | 単一プロセスHybrid版 | **2プロセス(start_eth0.php + start_eth1.php)Hybrid版** |
|---|---|---|
| UDP 700M×4並列(受信側) | 2.53 Gbps、loss 3.1% | **2.60 Gbps、loss 0.6%** |
| TCP -P 2(受信側) | 1.65 Gbps、retransmit 12 | **2.51 Gbps、retransmit 4,795** |

2プロセス化により、TCPが1.65Gbpsから2.51Gbpsへ大幅に向上した。read処理自体を2プロセス(≒2コア)に分散できたことに加え、各プロセスがそれぞれ独自のwriteスレッドを持つため、全体としてより多くのCPUコアを使えるようになったことが要因と考えられる。今回試した構成の中で最も高いスループットとなった。ただしTCPのretransmit数は単一プロセス版(12)より大幅に増えており(4,795)、スループットは上がった一方でロス/再送は増えているというトレードオフがある点は留意する。

検証後は、この2プロセス構成(`start_eth0.php`+`start_eth1.php`)を起動したままにしている。

## 今後の選択肢

- write側もAF_XDPで並列化する(`xsk_socket__create_shared`によるUMEM共有、plan.mdのOption A)を試し、2プロセスHybrid版をさらに上回れるか検証する。
- kernel ip_forward側の頭打ち要因(単一コア softirq、conntrack等)を`perf`/`mpstat`等で切り分け、今回のPHPルーターの数値がどの程度「妥当な上限」に近いのかを評価する。
- 実NIC(zero-copy対応ドライバ)環境での再検証。
- TCPのretransmit増加(2プロセス化の副作用)の原因調査。
